# GitLab Skill Dependencies
# Uses only standard library - no external dependencies required
#
# The gitlab_client.py uses urllib which is part of Python standard library
#
# If you need additional features, you can optionally install:
#
# requests>=2.28.0  # Alternative HTTP client (not required)